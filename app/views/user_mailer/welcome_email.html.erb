<!DOCTYPE html>
<html>
    <head>
      <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    </head>

    <body>
        <h1>Welcome to example.com, <%= @user.email %></h1>
        <p>
          You have successfully signed up to DeadSouls,
          your username is: <%= @user.email %>.<br>
        </p>
        <% if @user.type == 'Soul' %>
            <p>
              To login to the site, just follow this link: <%= link_to "Edit profile", edit_soul_url(@user.confirm_token) %>
            </p>
        <% else %>
            <p>
              To login to the site, just follow this link: <%= link_to "Edit profile", edit_laird_url(@user.confirm_token) %>
            </p>
        <% end %>
        <p>Thanks for joining and have a great day!</p>
    </body>
</html>